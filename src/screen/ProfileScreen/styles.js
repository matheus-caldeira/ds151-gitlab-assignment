import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    padding: 5,
  },

  image: {
    width: 200,
    height: 200,
    marginVertical: 15,
  }
});

export default styles;